package com.example.demo.service;

import com.example.demo.exception.BusinessException;
import com.example.demo.mapper.Issue64BigdecimalEmptyInitMapper;
import java.util.concurrent.atomic.AtomicReference;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

@Service
public class Issue64BigdecimalEmptyInitService {
    private static final Logger log = LoggerFactory.getLogger(Issue64BigdecimalEmptyInitService.class);

    private final Issue64BigdecimalEmptyInitMapper issue64BigdecimalEmptyInitMapper;

    public Issue64BigdecimalEmptyInitService(Issue64BigdecimalEmptyInitMapper issue64BigdecimalEmptyInitMapper) {
        this.issue64BigdecimalEmptyInitMapper = issue64BigdecimalEmptyInitMapper;
    }
    // Source: pkg_issue64_bd_init.proc_empty_string_defaults (PROCEDURE) — issue_64_bigdecimal_empty_init.sql:22-37
    public void procEmptyStringDefaults(String pId, AtomicReference<String> pResult) {
        String vOpenfundMode = "";
        String vOpenfundPriceFlag = "";
        String vPriceType = "";
        String vPgPrice = "";
        pResult.set(null);
        vOpenfundMode = "M";
        vPriceType = "T";
        pResult.set(String.valueOf(vOpenfundMode + vOpenfundPriceFlag + vPriceType + vPgPrice));
        if (pId != null) {
            pResult.set(String.valueOf((String) pResult.get() + pId));
        }
    }
}