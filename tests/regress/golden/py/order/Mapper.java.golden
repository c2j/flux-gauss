        package com.example.demo.mapper;

        import java.util.List;
import java.util.Map;

        import org.apache.ibatis.annotations.*;

        @Mapper
        public interface OrderMapper {

                // pkg_order.sql:1 — pkg_order.create_order
        int insertCreateOrder(@Param("pUserId") Long pUserId, @Param("pProductId") Long pProductId, @Param("pQty") Integer pQty);
        // pkg_order.sql:14 — pkg_order.cancel_order
        Map<String, Object> selectCancelOrder(@Param("pOrderId") Long pOrderId);
        // pkg_order.sql:14 — pkg_order.cancel_order
        int updateCancelOrder(@Param("pOrderId") Long pOrderId);
        // pkg_order.sql:31 — pkg_order.get_order_detail
        List<Map<String, Object>> selectGetOrderDetail(@Param("pOrderId") Long pOrderId);
        // pkg_order.sql:53 — pkg_order.complete_order
        int updateCompleteOrder(@Param("pOrderId") Long pOrderId);
        }