package com.example.demo.service;

import com.example.demo.exception.BusinessException;
import com.example.demo.mapper.Issue83OutCrossPkgMapper;
import com.example.demo.service.Issue83OutCrossPkgService;
import java.util.concurrent.atomic.AtomicReference;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import org.mockito.junit.jupiter.MockitoSettings;
import org.mockito.quality.Strictness;
import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.*;

@ExtendWith(MockitoExtension.class)
@MockitoSettings(strictness = Strictness.LENIENT)
class Issue83OutCrossPkgServiceTest {

    @Mock
    private Issue83OutCrossPkgMapper issue83OutCrossPkgMapper;

    @InjectMocks
    private Issue83OutCrossPkgService service;

    @Test
    @org.junit.jupiter.api.Timeout(value = 5, unit = java.util.concurrent.TimeUnit.SECONDS)
    void test_planIncrement_success() {
        String pSource = "test_pSource";
        AtomicReference<Long> pIdFrom = new AtomicReference<>(null);
        AtomicReference<Long> pIdTo = new AtomicReference<>(null);
        var _rowTmpl = new java.util.HashMap<String,Object>();
        service.planIncrement(pSource, pIdFrom, pIdTo);
    }

    @Test
    @org.junit.jupiter.api.Timeout(value = 5, unit = java.util.concurrent.TimeUnit.SECONDS)
    void test_buildIncremental_success() {
        String pRunId = "test_pRunId";
        AtomicReference<Long> pRows = new AtomicReference<>(null);
        AtomicReference<Long> pMonths = new AtomicReference<>(null);
        var _rowTmpl = new java.util.HashMap<String,Object>();
        service.buildIncremental(pRunId, pRows, pMonths);
    }

    @Test
    @org.junit.jupiter.api.Timeout(value = 5, unit = java.util.concurrent.TimeUnit.SECONDS)
    void test_runDaily_success() {
        String pRunId = "test_pRunId";
        var _rowTmpl = new java.util.HashMap<String,Object>();
        service.runDaily(pRunId);
    }
}