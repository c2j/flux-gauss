package com.example.demo.service;

import com.example.demo.exception.BusinessException;
import com.example.demo.mapper.Issue75Mapper;
import java.util.ArrayList;
import java.util.List;
import java.util.Map;
import java.util.concurrent.atomic.AtomicReference;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

@Service
// Source: {FIXTURES_ROOT}/issue_75_loop_exception_brace.sql
public class Issue75Service {
    private static final Logger log = LoggerFactory.getLogger(Issue75Service.class);

    private final Issue75Mapper issue75Mapper;

    public Issue75Service(Issue75Mapper issue75Mapper) {
        this.issue75Mapper = issue75Mapper;
    }

    // Source: pkg_issue75.proc_nested_block_body_fails (PROCEDURE) — {FIXTURES_ROOT}/issue_75_loop_exception_brace.sql:14-27
    public void procNestedBlockBodyFails(AtomicReference<String> pResult) {
        String vTmp = null;
        String __SQLERRM__ = "";
        int __SQLCODE__ = 0;
        try {
            // for r2 in cursor cInner
            List<Map<String, Object>> r2List = issue75Mapper.selectProcNestedBlockBodyFails();
            if (r2List == null) r2List = new ArrayList<>();
            for (Map<String, Object> r2 : r2List) {
                vTmp = String.valueOf(((java.util.Map<String, Object>) r2).get("v"));
            }
        } catch (Exception __e1) {
            __SQLERRM__ = __e1.getMessage();
            __SQLCODE__ = -1;
            vTmp = "fallback";
        }
        pResult.set(String.valueOf(vTmp));
    }

    // Source: pkg_issue75.proc_loop_exc_handler (PROCEDURE) — {FIXTURES_ROOT}/issue_75_loop_exception_brace.sql:29-46
    public void procLoopExcHandler(AtomicReference<String> pResult) {
        String vTmp = null;
        String __SQLERRM__ = "";
        int __SQLCODE__ = 0;
        // for rec in cursor cOuter
        List<Map<String, Object>> recList = issue75Mapper.selectProcLoopExcHandler();
        if (recList == null) recList = new ArrayList<>();
        for (Map<String, Object> rec : recList) {
            try {
                vTmp = String.valueOf(((java.util.Map<String, Object>) rec).get("v"));
            } catch (Exception __e1) {
                __SQLERRM__ = __e1.getMessage();
                __SQLCODE__ = -1;
                // for r2 in cursor cInner
                List<Map<String, Object>> r2List2 = issue75Mapper.selectProcLoopExcHandler_1();
                if (r2List2 == null) r2List2 = new ArrayList<>();
                for (Map<String, Object> r2 : r2List2) {
                    vTmp = String.valueOf(((java.util.Map<String, Object>) r2).get("v"));
                }
                continue;
            }
        }
        pResult.set(String.valueOf(vTmp));
    }
}