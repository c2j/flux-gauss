package com.example.demo.service;

import com.example.demo.exception.BusinessException;
import com.example.demo.mapper.PbizMapper;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

@Service
// Source: {FIXTURES_ROOT}/issue_71_parity_caller.sql
public class PbizService {
    private static final Logger log = LoggerFactory.getLogger(PbizService.class);

    private final PbizMapper pbizMapper;

    public PbizService(PbizMapper pbizMapper) {
        this.pbizMapper = pbizMapper;
    }

    // Source: PKG_PBIZ.do_it (PROCEDURE) — {FIXTURES_ROOT}/issue_71_parity_caller.sql:7-10
    public void doIt(String pIMsg) {
        // CALL PKG_PLOG.inst_log(pIMsg)
    }
}