package com.example.demo.service;

import com.example.demo.exception.BusinessException;
import com.example.demo.mapper.BigfundMapper;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

@Service
// Source: {FIXTURES_ROOT}/issue_70_fnc_a.sql
public class BigfundService {
    private static final Logger log = LoggerFactory.getLogger(BigfundService.class);

    private final BigfundMapper bigfundMapper;

    public BigfundService(BigfundMapper bigfundMapper) {
        this.bigfundMapper = bigfundMapper;
    }

    // Source: BIGFUND.FNC_A (FUNCTION) — {FIXTURES_ROOT}/issue_70_fnc_a.sql:1-6
    public String fncA(String pIX) {
        return String.valueOf(pIX).concat(String.valueOf("_A"));
    }
}