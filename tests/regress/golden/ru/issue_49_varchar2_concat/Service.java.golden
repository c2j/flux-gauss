package com.example.demo.service;

import com.example.demo.exception.BusinessException;
import com.example.demo.mapper.Issue49ConcatMapper;
import java.util.concurrent.atomic.AtomicReference;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

@Service
// Source: {FIXTURES_ROOT}/issue_49_varchar2_concat.sql
public class Issue49ConcatService {
    private static final Logger log = LoggerFactory.getLogger(Issue49ConcatService.class);

    private final Issue49ConcatMapper issue49ConcatMapper;

    public Issue49ConcatService(Issue49ConcatMapper issue49ConcatMapper) {
        this.issue49ConcatMapper = issue49ConcatMapper;
    }

    // Source: pkg_issue49_concat.proc_build_id_list (PROCEDURE) — {FIXTURES_ROOT}/issue_49_varchar2_concat.sql:46-73
    public void procBuildIdList(String pBaseIds, String pExtraIds, AtomicReference<String> pResult) {
        String vProId = null;
        if (pBaseIds != null) {
            vProId = String.valueOf(pBaseIds);
        } else {
            vProId = "6034";
        }
        vProId = String.valueOf(vProId).concat(String.valueOf("8208, 9, 354, 360, 370, 5821"));
        pResult.set(String.valueOf(vProId));
    }

    // Source: pkg_issue49_concat.proc_append_trade_ids (PROCEDURE) — {FIXTURES_ROOT}/issue_49_varchar2_concat.sql:78-93
    public void procAppendTradeIds(String pNewId, AtomicReference<String> pResult) {
        String vTradeIds = null;
        vTradeIds = "1001, 1002, 1003";
        pResult.set(String.valueOf(vTradeIds));
    }

    // Source: pkg_issue49_concat.proc_concat_labels (PROCEDURE) — {FIXTURES_ROOT}/issue_49_varchar2_concat.sql:98-110
    public void procConcatLabels(String pPrefix, String pSuffix, AtomicReference<String> pResult) {
        String vLabels = null;
        vLabels = String.valueOf(pPrefix);
        pResult.set(String.valueOf(vLabels));
    }
}