package com.example.demo.service;

import com.example.demo.exception.BusinessException;
import com.example.demo.mapper.Issue60InstrMapper;
import java.util.concurrent.atomic.AtomicReference;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

@Service
// Source: {FIXTURES_ROOT}/issue_60_instr_case_when.sql
public class Issue60InstrService {
    private static final Logger log = LoggerFactory.getLogger(Issue60InstrService.class);

    private final Issue60InstrMapper issue60InstrMapper;

    public Issue60InstrService(Issue60InstrMapper issue60InstrMapper) {
        this.issue60InstrMapper = issue60InstrMapper;
    }

    // Source: pkg_issue60_instr.proc_case_instr_when_zero (PROCEDURE) — {FIXTURES_ROOT}/issue_60_instr_case_when.sql:20-45
    public void procCaseInstrWhenZero(String pFileName, AtomicReference<String> pResult) {
        String vFileName = null;
        Integer vLen = 0;
        vFileName = String.valueOf(pFileName);
        vFileName = String.valueOf(vFileName).substring(Math.min(String.valueOf(vFileName).length(), Math.max(0, (3) - 1)), Math.min(String.valueOf(vFileName).length(), Math.max(0, (3) - 1) + ((String.valueOf(vFileName).indexOf("_") + 1.equals(0) ? String.valueOf(vFileName).indexOf(".dbf") + 1 - 3 : String.valueOf(vFileName).indexOf("_") + 1 - 3))));
        pResult.set(String.valueOf(vFileName));
        vLen = (String.valueOf(vFileName).indexOf(".") + 1.equals(0) ? 0 : String.valueOf(vFileName).indexOf(".") + 1);
    }

    // Source: pkg_issue60_instr.proc_instr_eq_zero (PROCEDURE) — {FIXTURES_ROOT}/issue_60_instr_case_when.sql:47-60
    public void procInstrEqZero(String pSrc, AtomicReference<String> pFound) {
        if (java.util.Objects.equals(String.valueOf(String.valueOf(pSrc).toLowerCase()).indexOf("ipogh") + 1, 0)) {
            pFound.set("N");
        } else {
            pFound.set("Y");
        }
    }
}