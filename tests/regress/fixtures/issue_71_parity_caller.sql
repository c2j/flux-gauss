CREATE OR REPLACE PACKAGE BIGFUND.PKG_PBIZ IS
    PROCEDURE do_it(p_i_msg IN VARCHAR2);
END PKG_PBIZ;
/

CREATE OR REPLACE PACKAGE BODY BIGFUND.PKG_PBIZ IS
    PROCEDURE do_it(p_i_msg IN VARCHAR2) IS
    BEGIN
        PKG_PLOG.inst_log(p_i_msg);
    END;
END PKG_PBIZ;
/
