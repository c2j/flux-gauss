# FluxGauss 使用指南

将 OpenGauss/PostgreSQL 存储过程（PL/pgSQL）自动转换为 Spring Boot + MyBatis Java 项目。

## 快速开始

### 1. 准备环境

- Python 3.9+（Python 引擎）或 Rust 1.80+（Rust 引擎）
- Java 17+（用于编译验证生成结果）
- ogsql 解析器（项目自带，或自行安装；Rust 引擎内建，无需单独安装）

### 2. 编写配置文件

在项目根目录创建 `fluxgauss.yaml`：

```yaml
output_dir: ./dest
base_package: com.example.demo

# 数据库连接（可选，用于生成 application.yml）
# database:
#   url: jdbc:postgresql://localhost:5432/mydb
#   username: app_user
#   password: secret
#   driver: org.postgresql.Driver

# 日志框架（可选，默认 slf4j）
# logger: log4j2

sources:
  - sql/pkg_order.sql
  - sql/pkg_product.sql
  - sql/pkg_payment.sql

# 自定义包映射（可选）— 将指定文件生成到不同的 Java 包下
# java_packages:
#   - package: com.example.order
#     sources:
#       - sql/pkg_order.sql
```

### 3. 执行转换

```bash
# Python 引擎（推荐，功能最完整）
python3 converter/flux_gauss.py -c fluxgauss.yaml

# Rust 引擎（大批量场景，性能更高）
cargo run --bin fluxgauss -- --config fluxgauss.yaml
```

### 4. 编译验证

```bash
cd dest
mvn compile        # 编译检查
mvn test           # 运行自动生成的单元测试
```

---

## 两种运行方式

### 方式一：配置文件（推荐）

```bash
# Python 引擎
python3 converter/flux_gauss.py -c fluxgauss.yaml

# Rust 引擎
cargo run --bin fluxgauss -- --config fluxgauss.yaml
```

适合源文件较多、需反复执行的场景，支持增量生成。Python 和 Rust 引擎共享同一 YAML 配置格式。

### 方式二：命令行参数（仅 Python 引擎）

```bash
python3 converter/flux_gauss.py -o <输出目录> -s <SQL文件1> [SQL文件2] ...
```

示例：

```bash
python3 converter/flux_gauss.py -o ./dest -s sql/pkg_order.sql sql/pkg_product.sql
```

适合一次性快速转换，**不支持增量生成**。

---

## 命令行选项

```
fluxgauss -c <config.yaml> | -o <dir> -s <sql> [...]

选项:
  -c, --config FILE     YAML 配置文件路径
  -o, --output DIR      输出目录（与 -s 配合使用）
  -s, --sources SQL...  SQL 源文件列表（与 -o 配合使用）
  --report FILE         指定转换报告输出路径（Markdown 格式）
  --full                强制全量重新生成（忽略缓存）
  --resume              从断点续做（跳过已成功生成的包）
  --skip-validate       跳过 SQL 语法校验阶段
  --encoding ENC        指定源码编码格式（默认 UTF-8，如 gbk）
  -v, --version         显示版本信息
  -h, --help            显示帮助信息
```

---

## 配置文件详解

```yaml
# 必填 ──────────────────────
output_dir: ./dest                    # 生成项目的输出目录
sources:                              # SQL 源文件列表（相对配置文件的路径）
  - sql/PACK_LOG.sql
  - sql/pkg_order.sql

# 可选 ──────────────────────
base_package: com.example.demo        # Java 基础包名，默认 com.example.demo

# 自定义 Java 包名映射（可选）
# 未在此处指定的文件使用 base_package
java_package:
  package: com.example.order
  sources:
    - sql/pkg_order.sql

# 如需映射多个不同的包，使用列表形式 java_packages：
# java_packages:
#   - package: com.example.order
#     sources:
#       - sql/pkg_order.sql
#   - package: com.example.inventory
#     sources:
#       - sql/pkg_inventory.sql

logger: slf4j                         # 日志框架，默认 slf4j
                                       # 可选: slf4j, log4j2, commons-logging, jul
                                       # 或自定义（见下方 logger 字段说明）

encoding: utf-8                        # 源码编码格式，默认 utf-8
                                       # 支持: utf-8, gbk, gb2312, big5 等

database:                             # 数据库连接，用于生成 application.yml
  url: jdbc:postgresql://host:5432/db
  username: user
  password: pass
  driver: org.postgresql.Driver       # 默认 org.postgresql.Driver
```

### java_package 字段说明

将指定的 SQL 源文件生成到自定义的 Java 包下，而不是默认的 `base_package`。

**单包映射（`java_package`）**：

```yaml
java_package:
  package: com.mybank.deposit
  sources:
    - sql/PKG_DEPOSIT.sql
    - sql/PKG_RPT_BATCH.sql
```

**多包映射（`java_packages`，列表形式）**：

```yaml
java_packages:
  - package: com.mybank.deposit
    sources:
      - sql/PKG_DEPOSIT.sql
  - package: com.mybank.loan
    sources:
      - sql/PKG_LOAN.sql
      - sql/PKG_LOAN_RPT.sql
```

| 字段 | 说明 |
|------|------|
| `package` | 自定义 Java 包名（完整包路径） |
| `sources` | 使用该包的 SQL 文件列表 |

> **注意**：更改 `java_package` 后需删除旧输出目录下对应的包文件，或使用 `--full` 重新生成。

### database 字段说明

| 字段 | 默认值 | 说明 |
|------|--------|------|
| `url` | `jdbc:postgresql://localhost:5432/demo` | JDBC 连接地址 |
| `username` | `postgres` | 数据库用户名 |
| `password` | `postgres` | 数据库密码 |
| `driver` | `org.postgresql.Driver` | JDBC 驱动类名 |

全部可选，不配置则使用默认值。如果需要连接 Oracle 数据库：

```yaml
database:
  url: jdbc:oracle:thin:@host:1521:orcl
  username: scott
  password: tiger
  driver: oracle.jdbc.OracleDriver
```

> **注意**：更换驱动后需同时修改 `pom.xml` 中的 JDBC 依赖。

### logger 字段说明

指定生成的 Java 代码中使用的日志框架。默认使用 `slf4j`。

**使用预设：**

```yaml
logger: log4j2
```

内置预设：

| 预设名 | 说明 | 额外 Maven 依赖 |
|--------|------|-----------------|
| `slf4j`（默认） | SLF4J + Logback（Spring Boot 自带） | 无 |
| `log4j2` | Apache Log4j 2 | `log4j-core`、`log4j-slf4j2-impl` |
| `commons-logging` | Apache Commons Logging | `commons-logging` |
| `jul` | Java Util Logging（JDK 内置） | 无 |

**自定义日志框架：**

```yaml
logger:
  imports:
    - "import com.mycompany.MyLogger;"
    - "import com.mycompany.MyLoggerFactory;"
  declaration: "private static final MyLogger log = MyLoggerFactory.create({class_name}.class);"
  pom:
    - "<dependency><groupId>com.mycompany</groupId><artifactId>my-logger</artifactId><version>1.0</version></dependency>"
```

| 字段 | 必填 | 说明 |
|------|------|------|
| `imports` | 是 | Logger 相关的 Java import 语句列表 |
| `declaration` | 是 | Logger 实例声明语句模板，`{class_name}` 会被替换为实际类名 |
| `pom` | 否 | 额外的 Maven `<dependency>` XML 片段列表，会注入生成的 `pom.xml` |

> **注意**：更换 logger 后需删除已有的 `pom.xml` 和 Service 文件重新生成，因为骨架文件只在不存在时写入。

### encoding 字段说明

指定 SQL 源文件和生成代码的编码格式。默认 `utf-8`。

**YAML 配置：**

```yaml
encoding: gbk
```

**命令行：**

```bash
python3 converter/flux_gauss.py -c fluxgauss.yaml --encoding gbk
```

| 编码 | 说明 |
|------|------|
| `utf-8`（默认） | 标准编码，适用于大多数场景 |
| `gbk` | 简体中文（GB2312 超集） |
| `gb2312` | 简体中文 |
| `big5` | 繁体中文 |

> **注意**：编码设置影响 SQL 源文件读取、生成的 Java/XML 文件写入，以及 `pom.xml` 中的 `project.build.sourceEncoding`。

---

## ogsql 解析器查找顺序

转换器按以下优先级查找 ogsql 可执行文件：

1. **当前目录** `./ogsql`
2. **环境变量** `$OGSQL_BIN`
3. **系统 PATH** 中的 `ogsql`
4. **项目内置路径** `lib/ogsql-parser/target/{arch}/release/ogsql`

默认无需配置，项目内置的解析器即可使用。如需使用自定义版本：

```bash
# 方式一：放在当前目录
cp /path/to/ogsql ./ogsql

# 方式二：设置环境变量
export OGSQL_BIN=/usr/local/bin/ogsql

# 方式三：加入 PATH
export PATH="/opt/ogsql/bin:$PATH"
```

---

## 双引擎

FluxGauss 提供两个独立的转换引擎，共享同一 YAML 配置格式：

| 维度 | Python 引擎 | Rust 引擎 |
|------|------------|-----------|
| 入口 | `converter/flux_gauss.py` | `cargo run --bin fluxgauss --` |
| 代码量 | ~14800 行 | ~15800 行 |
| 功能完整度 | 完整（评级 A-） | 持续对齐中（评级 B+） |
| 增量构建 | ✅ | ✅ |
| 集成测试生成 | ✅ | ✅ |
| 动态 SQL → MyBatis XML | ✅ | ✅ |
| 多编码支持 | ✅ | ✅ |
| 并行处理 | 单线程 | Rayon 多线程 |
| 适用场景 | 通用（100-1000 个 SP） | 大批量（1000-30000 个 SP） |
| ogsql 依赖 | 外部二进制（subprocess） | 内建 crate 依赖 |

两个引擎的输出结构完全相同，可用于交叉验证转换结果的正确性。

---

## SQL 语法校验

转换器在正式解析前会先执行 SQL 语法校验，提前发现源文件中的语法错误。

### 工作流程

1. 读取所有源文件
2. 逐文件调用 ogsql 解析器校验语法
3. 显示校验进度条
4. 如发现错误，输出错误详情并中止
5. 校验通过后进入解析和转换阶段

### 跳过校验

如果源文件包含非标准扩展语法（如 GaussDB 特有函数），校验阶段可能报错。此时可跳过校验：

```bash
python3 converter/flux_gauss.py -c fluxgauss.yaml --skip-validate
```

> **注意**：跳过校验后，语法错误可能在后续解析阶段暴露，错误信息可能不如校验阶段直观。

---

## 增量生成

使用配置文件模式时，自动启用增量生成。

### 工作原理

转换器在输出目录下维护 `.fluxgauss/` 缓存：

```
dest/
└── .fluxgauss/
    ├── manifest.json        # 每个源文件的内容哈希 + 包名映射
    └── ast/                 # 缓存的解析结果（JSON）
        ├── sql_pkg_order_sql.json
        └── ...
```

每次执行时：

1. 计算所有源文件的 **SHA-256 内容哈希**
2. 与缓存对比，确定哪些文件发生了变更
3. **跳过**未变更文件的解析（ogsql 调用）
4. 分析变更文件的**传递依赖**（如 A 调用 B，B 变更则 A 也重新生成）
5. 只重新生成受影响包的 Java/XML 文件

### 示例输出

**首次运行（全量）**：

```
    ███████╗██╗     ██╗   ██╗██╗  ██╗     ██████╗  █████╗ ██╗   ██╗███████╗███████╗
    ██╔════╝██║     ██║   ██║╚██╗██╔╝    ██╔════╝ ██╔══██╗██║   ██║██╔════╝██╔════╝
    █████╗  ██║     ██║   ██║ ╚███╔╝     ██║  ███╗███████║██║   ██║███████╗███████╗
    ██╔══╝  ██║     ██║   ██║ ██╔██╗     ██║   ██║██╔══██║██║   ██║╚════██║╚════██║
    ██║     ███████╗╚██████╔╝██╔╝ ██╗    ╚██████╔╝██║  ██║╚██████╔╝███████║███████║
    ╚═╝     ╚══════╝ ╚═════╝ ╚═╝  ╚═╝     ╚═════╝ ╚═╝  ╚═╝ ╚═════╝ ╚══════╝╚══════╝

  Output:    ./dest
  Config:    fluxgauss.yaml
  Input:     12 SQL file(s)

  Parsing: PACK_LOG.sql
  Parsing: pkg_order.sql
  ...
  Done! Packages: 11
```

**再次运行（无变更）**：

```
  Input:     12 SQL file(s)
  Incremental: 0 changed, 12 cached

  Cached: PACK_LOG.sql
  Cached: pkg_order.sql
  ...
  Incremental: regenerating 0/11 packages
```

**修改一个文件后**：

```
  Incremental: 1 changed, 11 cached

  Parsing: pkg_report.sql
  Cached: PACK_LOG.sql
  ...
  Incremental: regenerating 1/11 packages
```

**强制全量重新生成**：

```bash
python3 converter/flux_gauss.py -c fluxgauss.yaml --full
```

### 断点续做

当转换过程因错误中断时，可使用 `--resume` 从断点继续：

```bash
python3 converter/flux_gauss.py -c fluxgauss.yaml --resume
```

转换器在 `.fluxgauss/gen-checkpoint.json` 中记录已成功生成的包。使用 `--resume` 时，已成功的包会被跳过，只重新生成失败或未完成的包。

### 清除缓存

删除输出目录下的 `.fluxgauss/` 即可强制全量重新生成：

```bash
rm -rf dest/.fluxgauss
```

---

## 生成项目结构

```
dest/
├── pom.xml                                          # Maven 项目配置
├── src/
│   ├── main/
│   │   ├── java/com/example/demo/                   # base_package
│   │   │   ├── DemoApplication.java                 # Spring Boot 启动类
│   │   │   ├── exception/
│   │   │   │   └── BusinessException.java           # 业务异常
│   │   │   ├── mapper/
│   │   │   │   ├── OrderMapper.java                 # MyBatis Mapper 接口
│   │   │   │   └── ...
│   │   │   └── service/
│   │   │       ├── OrderService.java                # 业务 Service 类
│   │   │       └── ...
│   │   ├── java/com/example/order/                  # 自定义 java_package
│   │   │   ├── mapper/
│   │   │   │   └── DepositMapper.java
│   │   │   └── service/
│   │   │       └── DepositService.java
│   │   └── resources/
│   │       ├── application.yml                      # Spring Boot 配置
│   │       └── mapper/
│   │           ├── OrderMapper.xml                  # MyBatis XML 映射
│   │           └── ...
│   └── test/
│       └── java/
│           ├── com/example/demo/service/
│           │   ├── OrderServiceTest.java            # 单元测试
│           │   └── ...
│           └── com/example/order/service/
│               └── DepositServiceTest.java
└── .fluxgauss/                                      # 增量缓存（自动维护）
```

每个 SQL 文件（包）对应生成 4 个文件：

| 文件 | 说明 |
|------|------|
| `{PkgName}Service.java` | Service 类，包含包内所有存储过程对应的 Java 方法 |
| `{PkgName}Mapper.java` | MyBatis Mapper 接口，包含所有 DML 操作的方法签名 |
| `{PkgName}Mapper.xml` | MyBatis XML 映射文件，包含具体 SQL 语句 |
| `{PkgName}ServiceTest.java` | 基于 Mockito 的单元测试 |

---

## 源码溯源注释

生成的代码中包含源文件溯源注释，方便调试时定位原始 SQL：

### Java Service

```java
// Source: PKG_DEPOSIT_ACNT_INFO_INQUIRY.sql                    ← 类级别
// Source: pkg_deposit.prc_acnt_info_list (PROCEDURE) — PKG_DEPOSIT_ACNT_INFO_INQUIRY.sql:38-202   ← 方法级别
public List<Map<String, Object>> prcAcntInfoList(...) {
    try {
        ...
    } catch (Exception e) { // OTHERS — src: PKG_DEPOSIT_ACNT_INFO_INQUIRY.sql:38                  ← 异常处理
        ...
    }
}
```

### MyBatis Mapper 接口

```java
// PKG_DEPOSIT_ACNT_INFO_INQUIRY.sql:38 — pkg_deposit.prc_acnt_info_list
Object selectPrcAcntInfoList(@Param("pIUserId") String pIUserId, ...);
```

### MyBatis XML

```xml
<!-- Source: PKG_DEPOSIT_ACNT_INFO_INQUIRY.sql:38-202 — pkg_deposit.prc_acnt_info_list.selectPrcAcntInfoList -->
<select id="selectPrcAcntInfoList" resultType="java.util.LinkedHashMap">
    select ...
</select>
```

注释格式统一为 `文件名:起始行-结束行`，可直接在编辑器中搜索跳转。

---

## 支持的 PL/pgSQL 特性

| 特性 | 转换方式 |
|------|----------|
| 存储过程（PROCEDURE） | → Service 方法 |
| 函数（FUNCTION） | → 带返回值的 Service 方法 |
| IN / OUT / IN OUT 参数 | → 方法参数 / `AtomicReference<T>` |
| SELECT / INSERT / UPDATE / DELETE | → MyBatis Mapper 方法 + XML |
| MERGE INTO | → MyBatis `<update>` 标签（保留原生 MERGE 语法） |
| TRUNCATE | → Mapper `<update>` 调用 |
| IF / ELSIF / ELSE | → if / else if / else |
| FOR ... IN SELECT LOOP | → for 循环 + Mapper 查询 |
| WHILE LOOP | → while 循环 |
| 游标 OPEN / FETCH / CLOSE | → Mapper 查询 + 变量赋值 |
| EXCEPTION 处理（WHEN OTHERS） | → try-catch 块 |
| RAISE EXCEPTION | → BusinessException 抛出 |
| 跨包调用（pkg_other.proc） | → Service 注入 + 方法调用 |
| PRAGMA AUTONOMOUS_TRANSACTION | → `@Transactional(propagation = REQUIRES_NEW)` |
| %TYPE 锚定类型 | → 从 DDL 解析实际 Java 类型 |
| TABLE OF 类型 | → `java.util.List<ElemType>` |
| FORALL 批量操作 | → MyBatis `<foreach>` 批量或逐条循环 |
| 动态 SQL（EXECUTE IMMEDIATE） | → Mapper 调用（参数化）+ MyBatis 动态 XML 标签 |
| REFCURSOR OUT 参数 | → `List<Map<String, Object>>` 返回 |
| RETURNING INTO | → Mapper 调用 + 结果提取 |
| SELECT INTO | → Mapper 查询 + 变量提取 |
| GET DIAGNOSTICS row_count | → 影响行数变量赋值 |
| GOTO | → 状态机 / do-while + continue |
| 包常量（PACKAGE CONSTANTS） | → static final 字段 |

### 支持的内置函数

转换器将 GaussDB/OpenGauss 内置函数自动映射为 Java 等价逻辑：

#### 字符串函数

| SQL 函数 | Java 转换 |
|----------|----------|
| `substr` / `substring` | `String.substring()`（自动处理 1-based → 0-based） |
| `overlay` | 字符串拼接 + `substring()` |
| `position` | `String.indexOf() + 1` |
| `trim` / `ltrim` / `rtrim` | `trim()` / `replaceAll()` |
| `trim(LEADING/TRAILING/BOTH ... FROM)` | `replaceAll()` 按方向处理 |
| `upper` / `lower` | `toUpperCase()` / `toLowerCase()` |
| `length` | `String.length()` |
| `replace` | `String.replace()` |
| `instr` | `String.indexOf() + 1` |
| `concat` | `String.format()` |
| `lpad` / `rpad` | `String.format()` 填充 |
| `chr` / `ascii` | `(char)` / `(int) charAt(0)` |
| `left` / `right` | `substring()` 截取 |
| `reverse` | `StringBuilder.reverse()` |
| `repeat` | `String.repeat()` |
| `split_part` | `String.split()` |
| `initcap` | 首字母大写（Stream） |
| `translate` | 字符映射（Stream） |
| `regexp_replace` | `String.replaceAll()` |
| `regexp_like` | `String.matches()` |
| `convert` | `new String(bytes, encoding)` |

#### 数学函数

| SQL 函数 | Java 转换 |
|----------|----------|
| `abs` | `Math.abs()` |
| `ceil` / `floor` | `Math.ceil()` / `Math.floor()` |
| `round` | `Math.round()` |
| `trunc` | `Math.floor()` |
| `mod` | `%` 运算 |
| `power` | `Math.pow()` |
| `sign` | `Integer.signum()` |
| `sqrt` | `Math.sqrt()` |
| `log` | `Math.log()` |
| `exp` | `Math.exp()` |
| `greatest` / `least` | `Math.max()` / `Math.min()` |

#### 日期/时间函数

| SQL 函数 | Java 转换 |
|----------|----------|
| `current_timestamp` / `now` | `new java.sql.Timestamp(System.currentTimeMillis())` |
| `current_date` / `sysdate` | `new java.sql.Date(System.currentTimeMillis())` |
| `localtimestamp` | `java.sql.Timestamp.valueOf(LocalDateTime.now())` |
| `current_time` | `new java.sql.Timestamp(System.currentTimeMillis())` |
| `to_date` / `to_timestamp` | `java.sql.Date.valueOf()` / `java.sql.Timestamp.valueOf()` |
| `to_char`(日期) | `SimpleDateFormat.format()`（自动映射格式模板） |
| `to_char`(数值) | `DecimalFormat.format()`（自动映射格式模板） |
| `extract`(YEAR/MONTH/DAY/...) | `java.time` 字段访问 |
| `date_trunc` | `LocalDateTime.truncatedTo(ChronoUnit)` |
| `add_months` | `LocalDate.plusMonths()` |
| `last_day` | `LocalDate.withDayOfMonth()` |
| `next_day` | `LocalDate.plusWeeks()` |
| `months_between` | `Period.between().toTotalMonths()` |

#### 空值处理与条件

| SQL 函数 | Java 转换 |
|----------|----------|
| `nvl` | `args != null ? args : default` |
| `nvl2` | 三元表达式 |
| `coalesce` | `Objects.requireNonNullElse()` |
| `nullif` | `== ? null : value` |
| `decode` | 嵌套三元表达式链 |

#### 类型转换

| SQL 函数 | Java 转换 |
|----------|----------|
| `cast` | `(type) expr` |
| `to_number` | `Long.valueOf()` |
| `to_clob` | 原值透传 |

### 动态 SQL → MyBatis 动态 XML

转换器能自动检测 PL/pgSQL 中的条件 SQL 拼接模式，并将其转换为 MyBatis 动态 XML 标签（`<if>`、`<where>`、`<set>`）。

**示例 — PL/pgSQL 源码：**

```sql
v_sql := 'SELECT * FROM orders WHERE 1=1';
IF p_status IS NOT NULL THEN
    v_sql := v_sql || ' AND status = :p_status';
END IF;
IF p_date_from IS NOT NULL THEN
    v_sql := v_sql || ' AND create_date >= :p_date_from';
END IF;
```

**生成的 MyBatis XML：**

```xml
<select id="selectDynamic" resultType="java.util.LinkedHashMap">
    SELECT * FROM orders WHERE 1=1
    <if test="pStatus != null">
        AND status = #{pStatus}
    </if>
    <if test="pDateFrom != null">
        AND create_date &gt;= #{pDateFrom}
    </if>
</select>
```

支持的模式：
- 条件拼接（IF ... THEN v_sql := v_sql || ...）→ `<if>` 标签
- WHERE 子句动态条件 → `<where>` 标签自动处理前缀 AND/OR
- SET 子句动态更新 → `<set>` 标签自动处理末尾逗号
- 嵌套 concat 和 EXECUTE IMMEDIATE 前缀检测

---

## 注意事项

### 需要手动处理的情况

以下场景转换器会生成代码但可能需要人工审查：

- **动态 SQL**：复杂的字符串拼接条件，生成后需检查 SQL 语法
- **包级变量**：转译为 Service 内部的 `static` 变量，并发场景需评估线程安全
- **`to_char` 格式模板**：日期格式常见模板已支持自动映射，非标准格式需人工调整 `SimpleDateFormat` 模式；数字格式的复杂模板（如货币符号、科学计数法）可能需要手动补充
- **`translate` 函数**：生成的 Java Stream 表达式较冗长，性能敏感场景可考虑替换为手动循环
- **窗口函数**（`lag`/`lead`/`rank` 等）：留在 SQL 侧（MyBatis XML），不在 Java 层转换
- **聚合函数**（`listagg`/`wm_concat` 等）：同上，保留在 SQL 侧
- **存储过程重载**（同名不同参数）：会生成多个 Java 方法，方法名可能重复

### 编译验证

每次转换后建议执行 `mvn compile` 检查：

```bash
cd dest && mvn compile
```

如果有编译错误，检查错误信息中的文件名和行号，结合溯源注释定位原始 SQL 进行调整。

### 不覆盖已有骨架文件

`pom.xml`、`application.yml`、`DemoApplication.java` 等项目骨架文件只在**不存在时**生成。如需更新这些文件，请先删除后重新执行：

```bash
rm dest/pom.xml dest/src/main/resources/application.yml
python3 converter/flux_gauss.py -c fluxgauss.yaml
```

---

## 集成测试

转换完成后，除 Mock 单元测试外，可自动生成**连接真实数据库的集成测试**，验证 MyBatis SQL 在真实环境下的正确性。

### 启用方式

在 `fluxgauss.yaml` 中添加 `integration_test` 配置块：

```yaml
integration_test:
  enabled: true
  mode: testcontainers          # testcontainers | remote

  # Testcontainers 模式（推荐，需 Docker）
  image: opengauss/opengauss:latest

  # Remote 模式（直连真实数据库）
  # mode: remote
  # url: jdbc:postgresql://your-server:5432/testdb
  # username: test_user
  # password: test_pass

  # 额外的建表/初始化脚本（可选）
  init_sql:
    - sql/tables.sql
```

### 两种模式

| 模式 | 说明 | 适用场景 |
|---|---|---|
| `testcontainers` | 自动拉起 Docker 容器（默认 `opengauss/opengauss:latest`） | CI/CD、本地开发（需 Docker） |
| `remote` | 连接用户指定的数据库实例 | 测试真实环境、无 Docker 场景 |

### 运行方式

```bash
# 只跑 Mock 单元测试（不变）
cd dest && mvn test

# 跑集成测试（需 Docker 或远程数据库可达）
cd dest && mvn verify -Pintegration

# 先单元后集成
cd dest && mvn test && mvn verify -Pintegration
```

### 生成的文件

| 文件 | 说明 |
|---|---|
| `itest/AbstractIntegrationTest.java` | 公共基类（Testcontainers 启动 / 远程连接） |
| `itest/{Name}ServiceIntegrationTest.java` | 每包的集成测试 |
| `itest-schema.sql` | 自动从 SQL 源提取的建表 DDL |
| `itest-fixtures/*.sql` | 每个测试方法的测试数据（INSERT 语句） |
| `application-integration.yml` | 集成测试 Spring Profile 配置 |

### 自定义测试数据

自动推断的测试数据是**骨架**，复杂场景可能需要手动补充：

1. **编辑 fixture SQL**：修改 `src/test/resources/itest-fixtures/` 下的文件
2. **指定初始数据脚本**：在 YAML 中用 `init_sql` 引用
3. **补充断言**：在生成的 `*IntegrationTest.java` 中搜索 `// TODO` 标记，添加业务相关的断言

### 注意事项

- Testcontainers 需要 **Docker 环境**
- OpenGauss 容器启动较慢（约 30-60 秒），首次运行测试会较慢
- 自动推断的测试数据**不包含外键关联**，有外键的表需在 `init_sql` 中手动补充
- `itest-schema.sql` 中的 DDL 来自 `CREATE TABLE` 解析，复杂类型（分区表、继承表等）需在 `init_sql` 中补充
- `pom.xml` 只在**不存在时**生成，如需更新依赖需先删除

---

## 常见问题

### Q: 提示 `ogsql not found`

确认 ogsql 可执行文件存在。默认查找项目内置路径 `lib/ogsql-parser/target/`。也可通过环境变量指定：

```bash
export OGSQL_BIN=/path/to/ogsql
```

### Q: 提示 `(no procedures found)`

该 SQL 文件不包含存储过程定义，可能只是建表语句（DDL）或纯 SQL 脚本。这类文件仍需保留在 sources 列表中，因为其他存储过程可能依赖其中的表结构（用于 %TYPE 类型推断）。

### Q: 如何重新全量生成

```bash
rm -rf dest
# 或使用 --full 选项
python3 converter/flux_gauss.py -c fluxgauss.yaml --full
```

### Q: 如何只转换部分存储过程

修改 `fluxgauss.yaml` 的 `sources` 列表，只保留需要的 SQL 文件。未被引用的包文件会自动清除。

### Q: 如何修改生成的 Java 包名

修改配置文件中的 `base_package`：

```yaml
base_package: com.mycompany.myapp
```

然后删除旧输出重新生成：

```bash
rm -rf dest
python3 converter/flux_gauss.py -c fluxgauss.yaml
```

### Q: 如何让不同的 SQL 文件生成到不同的 Java 包下

使用 `java_package`（单个）或 `java_packages`（多个）配置：

```yaml
base_package: com.mybank.common
java_packages:
  - package: com.mybank.deposit
    sources:
      - sql/PKG_DEPOSIT.sql
  - package: com.mybank.loan
    sources:
      - sql/PKG_LOAN.sql
```

未在 `java_packages` 中列出的 SQL 文件使用 `base_package`。跨包调用（如 deposit 调用 common 的 Service）的 import 会自动解析为正确的包路径。

### Q: 如何查看转换报告

每次转换自动在 `.fluxgauss/reports/` 下生成带时间戳的 Markdown 报告，包含 SQL→Java 映射、跳过项、错误等信息。也可通过 `--report` 指定输出路径：

```bash
python3 converter/flux_gauss.py -c fluxgauss.yaml --report ./report.md
```

报告文件：
- `.fluxgauss/reports/conversion-report-YYYYMMDD_HHMMSS.md` — 历史报告
- `.fluxgauss/reports/conversion-report-latest.md` — 最新报告（每次覆盖）

### Q: 如何更换日志框架

修改配置文件中的 `logger` 字段（详见[logger 字段说明](#logger-字段说明)）。更换后需删除已有骨架文件重新生成：

```bash
rm dest/pom.xml
python3 converter/flux_gauss.py -c fluxgauss.yaml
```

### Q: Python 引擎和 Rust 引擎应该选哪个

- **Python 引擎**（推荐）：功能最完整，综合评级 A-，支持 25+ 种 PL/pgSQL 特性，适合绝大多数场景
- **Rust 引擎**：适合超大规模（1000+ SP）或需要并行处理的场景，综合评级 B+，功能仍在持续对齐

两个引擎共享相同的 YAML 配置格式，可以交叉验证输出结果。

### Q: 转换中途中断了怎么办

使用 `--resume` 从断点续做：

```bash
python3 converter/flux_gauss.py -c fluxgauss.yaml --resume
```

已成功生成的包（记录在 `.fluxgauss/gen-checkpoint.json` 中）会被跳过。

### Q: 如何跳过 SQL 语法校验

如果源文件包含非标准扩展语法导致校验报错，可使用 `--skip-validate` 跳过：

```bash
python3 converter/flux_gauss.py -c fluxgauss.yaml --skip-validate
```
